[project]
name = "cad-base"
version = "0.1.0"
description = "Base CAD development package"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    'build123d>=0.10.0',
    "cadquery-ocp-novtk>=7.9.3.1",
    'yacv-server==0.10.12',
]

[tool.setuptools]
py-modules = []