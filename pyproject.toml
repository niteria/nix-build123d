[project]
name = "devenv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    'build123d>=0.10.0',
    'yacv-server==0.10.12',
]
